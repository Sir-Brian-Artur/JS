const a = "1";              // === !== : Comparador Strict (Estricto)
const b = true;             // 1º. Comparamos el tipo de valor
                            // 2º. Comparamos el valor

                            // Le decimos a Javascript que no haga sus conversiones
console.log(a === b);       // de tipos, para intentar sacar un resultado.