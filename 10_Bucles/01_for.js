// El bucle for es el más común y se utiliza cuando sabes exactamente cuántas veces quieres repetir el código.

for (inicialización; condición; actualización) {
    // Código a ejecutar
}

for (let i = 0; i < 5; i++) {
    console.log(i); // Imprime 0, 1, 2, 3, 4
}