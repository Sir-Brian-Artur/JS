// Se utiliza para iterar sobre elementos de objetos iterables (como arrays, strings, etc.).

for (elemento of iterable) {
    // Código a ejecutar
}


const numeros = [1, 2, 3, 4, 5];

for (let numero of numeros) {
    console.log(numero); // Imprime 1, 2, 3, 4, 5
}