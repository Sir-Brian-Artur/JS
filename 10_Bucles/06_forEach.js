// Es un método DE LOS ARRAY que ejecuta una función para cada elemento del array.

array.forEach((elemento, indice, array) => {
    // Código a ejecutar
});


const frutas = ["manzana", "banana", "cereza"];

frutas.forEach((fruta, indice) => {
    console.log(`${indice}: ${fruta}`);
});
// Resultado:
// 0: manzana
// 1: banana
// 2: cereza