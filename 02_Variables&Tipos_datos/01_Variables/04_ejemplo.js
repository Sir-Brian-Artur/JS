const IVA = 1.21;

let precio = 25;
precio = precio * IVA;      //Reasignamos el valor de una variable 
                            //por el cálculo de su valor con una 
                            //constante, declaradas anteriormente.

let cantidad = 3;
let totalFactura = precio * cantidad;

//Asignamos a la variable "totalFactura" como e resultado de la 
//operación entre 2 variables.

console.log(totalFactura);