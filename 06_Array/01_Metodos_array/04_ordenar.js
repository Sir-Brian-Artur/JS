// 4. Métodos para ordenar y reorganizar

// Ordena los elementos del array (por defecto, como strings).

const array = [3, 1, 4, 2];
array.sort(); // [1, 2, 3, 4]


//Invierte el orden de los elementos del array.

const array2 = [1, 2, 3];
array2.reverse(); // [3, 2, 1]