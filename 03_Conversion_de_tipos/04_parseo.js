//Parsear se usa para pasar cualquier número STRING a NUMBER

parseInt();                 //Parte ENTERA
parseFloat();               //Parte DECIMAL

const cadena = "5.95";
const numero = 5;
const condicion = false;
const nulo = null;
let indefinido;

console.log(parseInt(cadena));      //Quita la parte decimal

console.log(parseFloat(cadena));    //Mantiene la parte decimal